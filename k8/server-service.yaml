apiVersion: v1
kind: Service
metadata:
  name: server-cluster-ip-service
  labels:  # Added for ServiceMonitor selection
    app: url-shortener
spec:
  type: ClusterIP
  selector:
    component: server
  ports:
    - name: http  # Added name for port reference
      port: 5000
      targetPort: 5000