server {
    listen 80;

    # Set the root to the static assets (e.g., the React build folder)
    root /usr/share/nginx/html; 
    index index.html;

    # This is CRUCIAL for client-side routing (e.g., React Router)
    location / {
        try_files $uri $uri/ /index.html;
    }
}